class AllExpensenItemModel {
  final String icon, date, amount, name;

  const AllExpensenItemModel(
      {required this.icon,
      required this.date,
      required this.amount,
      required this.name});
}
